# Model Scout Report — 2026-06-29

## Executive Summary

**3-day delta since 2026-06-26. The headline this cycle is not a new model — it's that source verification empties the carryover queue: three items the 06-26 report listed as pending/open are already DONE in the codebase.** No new sherpa-onnx release (still v1.13.3), no new Italian Whisper, no new audio-capable LLM, and every tracked GitHub issue is flat or closed.

The three resolved carryovers (verified against source, not the cached report):
1. **TASK-217 (Nemotron fp32→int8) is SHIPPED.** `NemotronDownloader.kt:36` resolves to `csukuangfj2/sherpa-onnx-nemotron-3.5-asr-streaming-0.6b-1120ms-int8-2026-06-11` (~640 MB int8), landed in commit `d45fe87`. The downloader comment reveals the fp32 predecessor had a **malformed decoder** (dangling `prednet_lengths` output — ORT refused to load it), so the int8 swap was a *correctness* fix, not just a size win. The int8 export is validated to load cleanly.
2. **The 06-26 "verify spec-decode bundle" item is RESOLVED.** `ModelDownloader.kt` pins `modelVersion = 2` for GEMMA_4_E2B/E4B with the comment *"v2 = monolithic .litertlm refreshed 2026-05-05 to bundle the MTP drafter"* — `isModelUpdateAvailable` uses the stamp to prompt stale v1 users to re-download. New downloads always get the current post-05-05 (spec-decode) file. **Yes, it pulls the bundle.**
3. **LiteRT-LM 0.12.0 → 0.13.1 is SHIPPED** (`build.gradle.kts:118`). Sub-Agent C confirms no newer stable; `MTP_SPECULATIVE_DECODING_ENABLED=false` keeps the user-facing prompt off until the runtime engages the drafter.

**What's genuinely new since 06-26 (low volume, as expected for a 3-day window):**
- The most interesting **new** finding is the **LocalAI-io Italian Whisper family** (github.com/localai-org/italian-asr): Italian-tuned Whisper fine-tunes trained on **YODAS spontaneous speech** (not just Common Voice), shipped as CTranslate2 INT8 + whisper.cpp GGML. It directly targets our input domain (voice messages = spontaneous speech) and claims to fix the "hallucinated repetitions on real audio" failure mode of CV-only Italian models. **Not sherpa-native → it's a runtime swap, not a drop-in. Watch, high interest.**
- **OpenASR Leaderboard credibility event**: Azure-Speech-05-2026 briefly took #1, then issue #170 (24 Jun) flagged a 10.10→8.08 single-month WER jump suggesting possible API benchmark contamination. Open-weight rankings (Granite, Cohere, **Parakeet TDT — best sub-1B**) are unaffected.
- **Granite Speech 4.1 definitively ruled out for Italian ASR**: its Italian support is **AST-only (translation TO Italian)**, not Italian ASR. Closes that question.
- **Qwen3-ASR official HF release** (`Qwen3-ASR-0.6B/1.7B-hf`, 26 Jun) landed — but no ONNX/sherpa export; community exports are empty-card/0-download. Watch issue #3535.

**Net actionable state: the carryover queue is empty. The single remaining real quality action is TASK-225 (SmoothQuant vs Stock int8 Parakeet Italian benchmark).**

## Framework Updates

### sherpa-onnx
- **Current (in-app):** v1.13.3
- **Latest release:** still **v1.13.3** (no v1.13.4)
- **Master since 06-26:** 4 commits in the window — all **QNN/Qualcomm-NPU** or build-packaging: `20ac56e` Whisper QNN runtime (#3699), `49ebd5b` ninja build fix (#3702), `46fac13` Windows non-ASCII path fix (#3255), `bbeb79e` espeak-ng-data install fix (#3708). **No new CPU ARM64 model support, no Italian/TDT-v3/int4/Granite/Canary work.** Only CPU-relevant item (ninja fix) doesn't affect our prebuilt AAR.
- **ORT in AAR:** still **1.26.0** (upstream 1.27.0 from 19 Jun has NOT entered the AAR).
- **Recommendation:** **No action.** Anti-Vocale is current; the csukuangfj2 int8 Nemotron path we ship is supported by the v1.13.3 streaming bindings (PR #3671).

### LiteRT-LM
- **Current (in-app):** **0.13.1** (bumped from 0.12.0 since 06-26 — the 06-26 recommendation #4 is done)
- **Latest:** 0.13.1 stable; **0.14.0-alpha.0** remains the latest prerelease (no 0.14 stable)
- **Changelog relevance:** 0.13.x adds MTP speculative-decoding runtime support (the `MTP_SPECULATIVE_DECODING_ENABLED` flag in `build.gradle.kts:45` is the gate; currently `false`). Note (Sub-Agent B): a Reddit report flags `enable_speculative_decoding` is wired in the OpenAI-server path but "never passed to Engine()" in the core Android `LlmManager` path we use — so spec-decode is *runtime-present but not yet engaged* for our audio path.
- **Recommendation:** **Hold at 0.13.1.** No newer stable to chase.

### llama-bro
- **Current (in-app):** fork `v1.3.0-gemma4` (disabled, `enable.gguf` property)
- **Upstream:** still **v1.2.3** (25 Mar 2026) — **dormant**. No release in the window. (Note: the upstream releases URL 404'd for both sub-agents — the org may have been renamed `whyisitworking` → `suhel-ai`; either way, no Gemma-4-audio movement.)
- **Gemma 4 GGUF audio path:** **still BLOCKED** (SDK profile + text-only GGUF weights). No fix, no new audio `.litertlm` from Gemma 4 beyond E2B/E4B.
- **Recommendation:** **No action.** Working audio path remains LiteRT-LM `.litertlm`.

### ONNX Runtime
- **Upstream latest:** v1.27.0 (19 Jun). **Not yet in the sherpa AAR** (still 1.26.0). Not directly actionable.

## ASR Model Findings (Delta)

### csukuangfj2 Nemotron 3.5 int8 — ALREADY SHIPPED (TASK-217 done)
- **Source:** https://huggingface.co/csukuangfj2/sherpa-onnx-nemotron-3.5-asr-streaming-0.6b-1120ms-int8-2026-06-11 (+ 80/160/560ms siblings)
- **In-app state:** `NemotronDownloader.kt` pulls the **1120ms int8** variant; `NemotronModelVariant.estimatedSizeMB = 640L`; backend comment *"Nemotron ships as int8"*. Landed in commit `d45fe87`.
- **Size:** ~640 MB int8 (vs ~2.45 GB fp32 — ~4× smaller)
- **Languages:** 40 language-locales incl. Italian `it`
- **sherpa-onnx compatible:** **YES** (csukuangfj = sherpa maintainer; canonical naming)
- **Italian quality:** UNVERIFIED for int8. Project memory: Nemotron had **word-skipping on Italian** (blankPenalty tuning helped slightly); not a Distil-IT replacement. The int8 swap was a *size + correctness* fix (the fp32 predecessor's decoder was malformed — ORT refused to load it), **not a quality change**. Gimics validated int8 ≈ fp32 bit-identical on en/de/ru; Italian not in their set.
- **Status:** **DONE.** Do not re-recommend.
- **Note:** HF shows ~0 downloads for the csukuangfj2 set — that understates reality since we ship it (sherpa's per-file fetch path isn't well-counted). We are an early adopter.

### Qwen3-ASR official HF release (0.6B + 1.7B, 26 Jun) — watch
- **Source:** https://huggingface.co/Qwen/Qwen3-ASR-0.6B-hf · https://huggingface.co/Qwen/Qwen3-ASR-1.7B-hf
- **Size:** 0.6B ~1.2 GB / 1.7B ~3.4 GB (safetensors, transformers-native)
- **Languages:** 30 langs + 22 Chinese dialects; **Italian officially supported**
- **sherpa-onnx compatible:** **NO** — transformers-native only. Community exports (`ilmina/qwen3-asr-1.7b-sherpa-onnx`, `andrewleech/qwen3-asr-1.7b-onnx`) have empty cards / 0 downloads / unverified provenance — skip until validated.
- **Composite:** 5/10. **Recommendation: Watch** issue #3535 for a csukuangfj 1.7B int8 export. (Our shipped 0.6b int8 is the weakest Italian backend ~12.2% WER; 1.7B may improve it but the size + no-sherpa-path rule it out today.)

### jeffpeng3 Nemotron onnx-int4-dynamic — watch
- **Source:** https://huggingface.co/jeffpeng3/nemotron-3.5-asr-streaming-0.6b-onnx-int4-dynamic (updated 06-21, 378 dl)
- **Format:** ONNX INT4 (block=128), single dynamic encoder for all chunk sizes
- **sherpa-onnx compatible:** **uncertain** — genai_config layout, not canonical sherpa OnlineRecognizer layout
- **Composite:** 6/10. **Recommendation: Watch.** Most size-aggressive Nemotron (~350–400 MB est.) with real adoption, but would need a custom adapter. A *sherpa-native* int4 would be the unlock; doesn't exist yet.

### LocalAI-io Italian Whisper family — watch (highest-interest NEW find)
- **Source:** https://github.com/localai-org/italian-asr
- **Models:** `whisper-large-v3-turbo-it-yodas-only` (best for real meetings/calls/podcasts), `whisper-large-v3-it-multi` (audiobooks/news), `vibevoice-asr-it-yodas-only` (Qwen2-based, native diarization)
- **Format:** CTranslate2 INT8 + whisper.cpp GGML (q4_0/q5_0/q8_0) + safetensors; **MIT license**
- **Key differentiator:** trained on **YODAS** (real spontaneous speech), explicitly claiming to fix *"hallucinated repetitions on real audio"* that CV-only Italian Whisper models suffer — directly targets our voice-message domain.
- **sherpa-onnx compatible:** **NO** — CTranslate2/whisper.cpp runtime, not ONNX/sherpa. Integration = a **runtime swap** (new backend), not a drop-in.
- **Composite:** 7/10 (Italian quality potential high, but integration effort high + runtime divergence)
- **Recommendation: Watch / evaluate offline.** Worth a manual WER test on our real WhatsApp-audio corpus vs Distil-IT before committing to a runtime swap. If the spontaneous-speech claim holds, this is the most promising Italian-quality path since bofenghuang's distil.

### Other ASR (skip, logged for completeness)
- `litert-community/wav2vec2-base-960h-CTC-LiteRT` (06-28, 190 MB) — English-only, LiteRT (not LiteRT-LM) runtime; proof-of-concept GPU-resident CTC. Skip.
- `Recogment/parakeet-multitalker-int8-onnx` (06-25) — diarization, English-only. Wrong scope. Skip.
- `qualcomm/Distil-Whisper` (06-16) — Snapdragon-NPU/AI-Engine specific, not sherpa/ORT CPU. Skip.
- `Yiivgeny/parakeet-tdt-0.6b-v3-sherpa-onnx-fp16/fp32` — reference-only; we already ship smaller int8. No benefit.
- **Whisper Italian:** still dormant since bofenghuang's distil-large-v3-it. No new Italian fine-tune.

## LLM Model Findings

### No new audio-capable LLM since 06-26
The only audio-capable `.litertlm` models remain the **Gemma 4 E2B/E4B** family we ship (Gemma 4 12B is desktop-class + weak transcription, 8.8% AA-WER #58 — skip).

### litert-community Gemma-4 E2B/E4B .litertlm — spec-decode bundle CONFIRMED handled
- **Source:** https://huggingface.co/litert-community/gemma-4-E2B-it-litert-lm (E2B 2.59 GB, ~1.07M dl/mo) · `gemma-4-E4B-it-litert-lm` (3.66 GB, ~426K dl/mo)
- **No content change since 06-26** (last real file update ~05-05; 06-05 was a README refresh with spec-decode benchmark tables already captured 06-26).
- **Spec-decode bundle status: handled.** `ModelDownloader` pins `modelVersion = 2` → `isModelUpdateAvailable` prompts stale v1 users to re-download → they get the post-05-05 MTP-drafter bundle. New downloads always get current `main`. **The 06-26 open verification item is closed: YES, the bundle is pulled.**
- **Caveat:** no git `revision` is pinned — if litert-community refreshes the file again, a maintainer must bump `modelVersion` or existing users won't be prompted. Minor maintenance note, not a problem today.

### litert-community burst (06-27→06-29) — ~95% vision, skip for us
~25 new models, almost all vision (D-FINE, RT-DETR, RTMPose, MI-GAN, real-esrgan, etc.). Only one ASR model (wav2vec2-base-960h, English-only, wrong runtime — see above). New text-only reasoning `.litertlm` models (Phi-4-mini-reasoning, Qwen3-4B-Thinking-2507, VibeThinker-3B) are **text-only, no audio** — useful only as conversion-recipe data points (int4 block size is arch-specific: block-32 for Phi-4-mini, block-128 for Qwen3-4B).

### llama-bro / Gemma 4 GGUF audio
Still **BLOCKED**. Upstream dormant; no audio multimodal work. Text-only OBLITERATUS GGUFs (`MisterPotato92/gemma-4-E2B-it-qat-q4_0-gguf`, etc.) keep appearing — all `supportsAudio = false`. **GGUF path remains text-only; `.litertlm` is the only audio path.**

## Tracked Items

| Item | Status | Change since 06-26 |
|------|--------|-------------------|
| sherpa-onnx v1.13.3 | Current in-app | No v1.13.4; master is QNN + build fixes |
| **TASK-217** Nemotron fp32→int8 | **DONE** | Shipped in `d45fe87`; csukuangfj2 1120ms int8 (~640 MB); fp32 decoder was malformed |
| **TASK-225** SmoothQuant vs Stock benchmark | **Pending** | **Now the single remaining real quality action** |
| **TASK-224** SmoothQuant Parakeet | DONE (prior) | Unchanged |
| VibeVoice TTS (#3106) | **Flat** | Open, no comments/labels/PR since Jan |
| Qwen3-ASR-1.7B sherpa (#3535) | **Flat** | 69 days open, zero maintainer response; official HF release (26 Jun) has no sherpa export |
| Cohere Transcribe sherpa (#3442) | **Closed** | Closed via PR #3456 (Apr) — **drop from active watch** (06-26 "flat" was stale) |
| LiteRT-community TFLite ASR | Minor add | Only `wav2vec2-base-960h` (EN-only, wrong runtime); no new audio-LLM |
| Gemma 4 GGUF backend | **Blocked** | llama-bro upstream dormant; no fix |
| **Spec-decode bundle (06-26 open)** | **RESOLVED** | `ModelDownloader modelVersion=2` handles it — YES, pulled |
| **LiteRT-LM 0.13.1 bump (06-26 #4)** | **DONE** | `build.gradle.kts:118` = 0.13.1 |

## Landscape Developments

**OpenASR Leaderboard credibility event.** Azure-Speech-05-2026 briefly took #1 on both main and multilingual boards (~8 Jun), then **issue #170 (24 Jun)** flagged its private WER jumping 10.10→8.08 and conversational 18.18→14.63 in a *single monthly release* — possible benchmark contamination by the continuously-updated API. The issue is open/unanswered. **Implication: treat cloud-API leaderboard rankings as unreliable; open-weight standings (Granite #1, Cohere #2, Parakeet TDT best sub-1B) are unaffected.** We could not scrape the live HF Space (human-verification wall), so the 2026-05-22 snapshot remains our reference.

**Granite Speech 4.1 — closed for Italian ASR.** Confirmed its language coverage is ASR = EN/FR/DE/ES/PT/JA only; **Italian is AST-only (translation TO Italian)**, not Italian ASR. Rules it out as an Italian candidate regardless of size. (Apache 2.0, dual-head CTC + frame importance sampling — interesting architecture if IBM ever extends Italian ASR.)

**X-ASR landed in sherpa-onnx (QNN-only).** PRs #3656/#3662/#3666/#3668 add a new "X-ASR" model family with Android QNN demos. No HF page surfaced — unclear what it is or whether it covers Italian. Worth identifying next cycle.

**NPU acceleration continuing to mature (not today).** First TDT Parakeet on Snapdragon NPU shipped 23 Jun (sherpa-onnx-qnn-parakeet-tdt-0.6b-v2, QNN). NexaML running Parakeet TDT on Hexagon NPU described as "new gold standard for on-device ASR." None sherpa-CPU-direct; confirms the trajectory for future devices. Want a v3 + Italian/multilingual QNN build.

**No new Italian ASR benchmark** in the window. Nemotron 3.5 including it-IT natively remains the biggest recent Italian-ASR event (already shipped). Common Voice 25.0 Italian (09 Mar) is the dataset baseline for Italian fine-tunes.

## Prioritized Recommendations

The carryover queue is empty. Only TASK-225 and one new evaluation remain.

| # | Action | Impact | Effort | Risk | Status |
|---|--------|--------|--------|------|--------|
| 1 | **TASK-225** — Benchmark SmoothQuant (862 MB) vs Stock (464 MB) int8 Parakeet on long Italian audio | high | low | low | **Pending — the single remaining real quality question** |
| 2 | **Evaluate (offline)** LocalAI-io Italian Whisper (YODAS) on our real WhatsApp-audio corpus vs Distil-IT before any runtime-swap decision | med–high | med | med | New — verify the spontaneous-speech claim before committing to a non-sherpa runtime |
| 3 | Watch `sherpa-onnx-qnn-parakeet-tdt` (NPU), Qwen3-ASR-1.7B sherpa (#3535), X-ASR family, jeffpeng3 int4 Nemotron | low | — | — | Watch only |

## Watch List
- **LocalAI-io Italian Whisper (YODAS)** — highest-interest Italian-quality candidate; non-sherpa runtime (CTranslate2/whisper.cpp). Test WER on real audio before a backend swap.
- **sherpa-onnx v1.13.4** — first point release after 1.13.3; look for streaming/int4/new-model work (master is currently QNN-only).
- **Qwen3-ASR-1.7B sherpa int8** (#3535) — #8 on OpenASR; the accuracy-upgrade path if csukuangfj exports it.
- **X-ASR family** in sherpa-onnx — unidentified new model family (QNN-only today); determine coverage/Italian.
- **jeffpeng3 int4 Nemotron** — real demand for sub-460 MB Nemotron; a sherpa-native int4 would unlock it.
- **OpenASR issue #170 resolution** — watch for HF team's stance on API-model leaderboard safeguards.
- **Granite Speech 4.1 `-nar`** — non-autoregressive, high-throughput; revisit only if IBM adds Italian *ASR* (currently AST-only).
- **ONNX Runtime 1.27.0** — will eventually land inside a sherpa AAR (1.13.3 still bundles 1.26.0).

## Comparison with Previous Report (2026-06-26)
- **Carryover queue emptied by source verification:** TASK-217 (Nemotron int8) **DONE** (`d45fe87`); spec-decode-bundle verification **RESOLVED** (modelVersion=2); LiteRT-LM 0.13.1 bump **DONE**. The 06-26 report listed all three as pending/open — the *report* was stale, the *code* had moved.
- **TASK-225 (SmoothQuant benchmark)** — still pending; now the lone remaining real action.
- **New watch (highest interest):** LocalAI-io Italian Whisper (YODAS spontaneous-speech fine-tunes) — directly targets our voice-message domain; non-sherpa runtime.
- **Landscape:** OpenASR credibility event (Azure-Speech disputed via issue #170); Granite Speech 4.1 Italian ruled out (AST-only); X-ASR landed in sherpa (QNN-only, unidentified).
- **Unchanged:** No new sherpa-onnx release; no new Italian Whisper in the sherpa ecosystem; no new audio-capable LLM; #3106 and #3535 flat.
- **Correction to 06-26:** Cohere sherpa #3442 is **Closed** (PR #3456, Apr) — 06-26's "flat" was stale; drop from active watch.

## No Change
- **sherpa-onnx** — still v1.13.3 (master QNN + build fixes)
- **Whisper Italian (sherpa ecosystem)** — dormant since bofenghuang's distil-large-v3-it
- **llama-bro / Gemma 4 GGUF audio path** — blocked; upstream dormant
- **VibeVoice TTS (#3106), Qwen3-1.7B (#3535)** — flat
- **ONNX Runtime in sherpa AAR** — still 1.26.0 (1.27.0 upstream only)
- **Audio-capable LLM inventory** — still Gemma 4 E2B/E4B only; no new entrant
